<%= form_with(model: twiter) do |form| %>
  <% if twiter.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(twiter.errors.count, "error") %> prohibited this twiter from being saved:</h2>

      <ul>
        <% twiter.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :user_name, style: "display: block" %>
    <%= form.text_field :user_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
